# inventory-manager
Para criar as imagens
  - docker-compose up -d

Para subir o backend
  - docker exec -it node_inventory_manager ash
  - está dentro do container e pode roudar no terminal normalmente
  - npm run dev
